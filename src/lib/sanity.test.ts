import { describe, expect, it } from "vitest";

// Placeholder até o M1 trazer os testes reais do window manager.
describe("test runner", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
