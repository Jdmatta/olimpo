# Dependencies / build output
node_modules
dist
dist-ssr
*.local

# Rust build output (src-tauri/target has its own gitignore via cargo, be explicit anyway)
src-tauri/target

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Local data — never commit databases or secrets
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.*

# Assinatura do updater — chave privada e artefato gerado por release
*.key
latest.json
docs-estudo/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
